# Changelog

All notable changes to KnightSchool will be documented in this file. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); auto-managed by [`standard-version`](https://github.com/conventional-changelog/standard-version) — `npm run release` regenerates this from the Conventional Commits log and writes the new version + tag.

## Unreleased

Pre-1.0 commit history (steps 1–10 of the build plan) is captured directly in `git log`. The first `npm run release` will fold every commit since the initial `feat:` into the v1.0.0 entry below.
